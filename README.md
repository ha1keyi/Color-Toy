# Color Toy (Recovered Source)

This branch contains source files reconstructed from `assets/index-WaiUA6Mb.js.map` (`sourcesContent`).

## Development

1. Install dependencies:

   npm install

2. Start dev server:

   npm run dev

## Quality Gates

- Type checking: `npm run typecheck`
- Linting: `npm run lint`
- Tests: `npm run test:run`
- Full verification: `npm run check`

## Project Audit and Upgrade Plan

- See `docs/project-upgrade-plan.md` for the engineering and product upgrade roadmap.

## Notes

- This is a source-first branch intended for ongoing development.
- Previous preview build snapshot is preserved on `master`.
